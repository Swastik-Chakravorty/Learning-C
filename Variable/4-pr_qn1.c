#include <stdio.h>

int main()
{
    int a, b;
    printf("Enter the value of Lentgh in cm \n");
    scanf("%d", &a);

    printf("Enter the value of Breadth in cm \n");
    scanf("%d", &b);

    printf("Area of the rectangle is %d cm", a * b);
    
    return 0;
}