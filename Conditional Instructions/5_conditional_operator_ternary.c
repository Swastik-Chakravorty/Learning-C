#include<stdio.h>

int main()
{
    int a;
    printf("Enter avlue of A ");
    scanf("%d", &a);

    // One liner if-else operator
    (a<5)? printf("A is less than 5") : printf("A is not less than 5");
    return 0;
}
