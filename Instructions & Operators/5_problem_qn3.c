#include<stdio.h>

int main()
{
    int num;
    int x = 2, y = 3, z = 3, k = 1;
    printf("Enter the Number \n");
    scanf("%d", &num);
    printf(" Divisibility test returns: %d \n", num%97);
    printf(" Result will be: %d \n", 3 * x / y - z + k);
    return 0;
}
